# 1. Open /etc/rsyslog.conf and add "$ModLoad imfile" before the line "$IncludeConfig" in /etc/rsyslog.d/*.conf
# 2. curl https://raw.githubusercontent.com/mhanief/workcloud_nginx/main/nginx.conf > /etc/rsyslog.d/nginx.conf
# 3. Restart nginx
